import '../css/App.css';
import Form from "../components/Form"

const Home = () => {
    return (
        <div className={"container"}>
            <h1>Add a card to the collection!</h1>
            <Form />
        </div>
    )
}

export default Home
